请重构系统总览页面中的“智能体协作星图”模块，实现一个具有银河系、太阳系空间感的七阶段质量闭环动画。

不要只生成静态样式，需要实现真实、连续、流畅的前端动画。

一、业务流程

中心是“当前项目/全部项目”，中心节点保持固定，不参与旋转。

外围包含七个阶段，必须严格按照以下先后顺序排列并循环：

01 需求分析
→ 02 测试设计
→ 03 脚本生成
→ 04 自动执行
→ 05 结果分析
→ 06 失败修复
→ 07 回归验证
→ 返回 01 需求分析

箭头必须沿银河轨道指向下一阶段，不能指向中心项目。

具体箭头关系：

需求分析 → 测试设计
测试设计 → 脚本生成
脚本生成 → 自动执行
自动执行 → 结果分析
结果分析 → 失败修复
失败修复 → 回归验证
回归验证 → 需求分析

二、整体视觉方向

视觉主题为“银河系中的智能质量闭环”。

背景使用深蓝、靛蓝、紫色组成的银河星空，包含：

1. 中央星云；
2. 多层椭圆形轨道；
3. 少量大小不同的星点；
4. 缓慢移动的光尘粒子；
5. 极弱的星云呼吸效果；
6. 不要过度闪烁，避免影响文字阅读。

中心项目节点设计成发光的能量核心或恒星：

- 中间显示“协作核心”；
- 主标题显示“全部项目”或当前项目名称；
- 显示“7 个阶段流转中”；
- 有蓝紫色能量环；
- 能量环缓慢旋转和呼吸；
- 不要让中心节点遮挡外围轨道。

三、阶段节点样式

不要继续使用普通白色矩形卡片。

每个阶段设计成“星云能量体”“行星标签”或“悬浮光团”，建议结构：

- 半透明深色玻璃材质；
- 不规则圆角或椭圆轮廓；
- 外围有同色系能量环；
- 内部显示阶段编号、图标、名称、状态和数量；
- 图标具有轻微发光；
- 节点附近可有少量环绕粒子；
- 节点与银河背景融合，但文字必须清晰。

建议阶段颜色：

01 需求分析：蓝色
02 测试设计：绿色
03 脚本生成：青绿色
04 自动执行：亮蓝色
05 结果分析：橙色
06 失败修复：紫色
07 回归验证：黄绿色

当前焦点阶段使用对应颜色的高亮能量效果，不要所有节点同时高亮。

四、空间运动效果

七个阶段围绕中心沿椭圆轨道顺时针旋转。

必须表现明显的“由远及近”和“由近及远”空间层次。

远处节点：

- scale 约为 0.45～0.60；
- opacity 约为 0.35～0.55；
- blur 约为 1～2px；
- 亮度较低；
- 字体和图标相对较小；
- z-index 较低；
- 移动速度视觉上略慢。

接近前景时：

- 节点逐渐变大；
- opacity 逐渐增加；
- blur 逐渐消失；
- 发光强度逐渐增强；
- 文字逐渐清晰；
- z-index 逐渐提升；
- 产生类似星星从宇宙深处飞近观察者的感觉。

到达最近位置时：

- scale 约为 1.15～1.30；
- opacity 为 1；
- blur 为 0；
- 节点成为当前焦点；
- 光晕和能量环最明显；
- 但不要突然放大，要连续平滑过渡。

离开前景后：

- 节点逐渐缩小；
- 亮度逐渐降低；
- 透明度逐渐降低；
- 重新进入轨道远端。

不要让七个节点同时放大。任何时刻只允许一个主要焦点节点，前后相邻节点可以轻度突出。

五、箭头和轨道动画

箭头必须沿轨道切线方向运动，不能使用从节点指向中心的放射线。

箭头样式建议：

- 使用银河光流或流星轨迹；
- 箭头由短光线、粒子和柔和箭头头部组成；
- 沿顺时针方向流动；
- 每一段轨道只从当前阶段流向下一阶段；
- 光点沿轨道移动，形成流程数据正在传递的感觉；
- 颜色以蓝白色为主；
- 当前阶段到下一阶段的箭头可以使用当前阶段颜色加强；
- 箭头不能太粗、太亮，避免抢过节点。

轨道至少分为三层：

1. 主流程轨道：显示七阶段箭头；
2. 能量装饰轨道：缓慢反向旋转；
3. 星尘轨道：粒子缓慢漂移。

箭头的起点和终点需要落在相邻节点之间，不能穿过节点，也不能指向中央项目。

六、焦点切换逻辑

整个动画建议 14～18 秒完成一圈。

每个阶段成为前景焦点时，保持约 1.2～1.8 秒，然后继续移动。

当前焦点阶段发生变化时：

1. 上一个焦点逐渐缩小并离开前景；
2. 下一个阶段沿轨道逐渐靠近；
3. 下一个阶段的光晕增强；
4. 下方“当前阶段详情区”同步切换内容；
5. 切换过程不要跳变。

下方阶段详情区域显示：

- 当前阶段名称；
- 当前状态；
- 阶段编号，例如 5 / 7；
- 负责人；
- 关联任务数；
- 风险数量；
- 预计完成时间；
- 阶段说明。

七、交互要求

1. 鼠标悬停在星图区域时，整体动画减速或暂停；
2. 鼠标悬停某个阶段时，该节点轻微放大，并显示详细信息；
3. 点击某个阶段后：
   - 将该阶段平滑旋转到前景；
   - 暂停自动轮播 5 秒；
   - 更新下方阶段详情；
4. 点击中心项目节点，可以进入项目详情；
5. 支持“暂停/播放”按钮；
6. 支持“2D/3D 视图”切换；
7. 支持 prefers-reduced-motion，用户关闭动画时显示静态环形流程；
8. 页面切换到后台时暂停动画，返回页面后继续。

八、技术实现建议

优先使用现有前端技术栈。

如果项目是 React + TypeScript：

- 使用 React 组件；
- 使用 requestAnimationFrame 驱动轨道位置；
- 使用 CSS transform: translate3d、scale、rotate；
- 使用 perspective 和 transform-style: preserve-3d；
- 使用 SVG 绘制椭圆轨道和箭头路径；
- 使用 SVG path + stroke-dashoffset 实现流光箭头；
- 使用 Canvas 或轻量粒子库实现星尘；
- 可以使用 Framer Motion 或 GSAP，但不要引入过重依赖；
- 不建议仅依赖多个 setInterval；
- 动画状态和业务状态分离。

推荐组件拆分：

GalaxyWorkflow
├── GalaxyBackground
├── ProjectCore
├── OrbitLayer
├── FlowArrowLayer
├── StageNode
├── ParticleLayer
├── StageDetailPanel
└── AnimationControls

每个节点的位置根据椭圆轨道角度计算：

x = centerX + radiusX * cos(angle)
y = centerY + radiusY * sin(angle)

使用 y 或 sin(angle) 计算空间深度：

depth = (sin(angle) + 1) / 2

再根据 depth 动态计算：

scale
opacity
blur
brightness
zIndex
shadow
glowIntensity

要求节点在轨道后方时更小、更暗、更虚，在轨道前方时更大、更亮、更清晰。

九、动画节奏

不要使用机械的匀速旋转。

建议：

- 整体轨道缓慢持续旋转；
- 节点靠近前景时轻微减速；
- 成为焦点后短暂停留；
- 离开前景时缓慢加速；
- 使用 easeInOutSine、power2.inOut 或类似缓动；
- 星尘粒子和背景轨道使用不同速度，产生视差；
- 中央能量环使用独立的慢速旋转；
- 当前焦点节点有非常轻微的呼吸缩放，范围控制在 1～1.03。

十、布局约束

保留当前页面总体结构：

- 左侧系统菜单；
- 顶部标题和操作按钮；
- 顶部指标卡；
- 中间银河流程主视觉；
- 右侧今日质量简报；
- 底部当前阶段详情。

银河主视觉需要成为页面重点，但不能影响右侧风险简报的可读性。

在 1440×900 和 1920×1080 分辨率下保持完整展示。

小屏幕下：

- 可以缩小轨道；
- 隐藏部分装饰粒子；
- 但不能改变七阶段顺序；
- 不能让阶段节点互相重叠。

十一、性能要求

- 动画目标帧率 60 FPS；
- 避免频繁触发布局计算；
- 优先改变 transform 和 opacity；
- 粒子数量根据设备性能动态调整；
- 页面不可见时停止 requestAnimationFrame；
- 禁止产生明显内存泄漏；
- 不要因为星空动画影响表格、菜单和按钮操作。

十二、验收标准

1. 中心项目固定，七个阶段围绕中心旋转；
2. 阶段顺序严格为：
   需求分析 → 测试设计 → 脚本生成 → 自动执行 → 结果分析 → 失败修复 → 回归验证；
3. 箭头只指向下一阶段，不指向中心；
4. 节点有明显的远近空间变化；
5. 节点从远处靠近时由小变大、由暗变亮、由虚变清晰；
6. 最近的节点最大，远处节点最小；
7. 当前阶段和下方详情同步；
8. 点击阶段可平滑移动到前景；
9. 悬停时可以减速或暂停；
10. 动画流畅，没有跳动、闪烁和节点重叠；
11. 星云背景梦幻，但文字始终清晰；
12. 不使用传统白色矩形卡片作为阶段主体。