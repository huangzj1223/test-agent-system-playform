# 🚀 导出功能快速启动指南

## 立即开始使用导出功能

### 步骤 1: 安装依赖

```bash
# 后端依赖
cd backend
uv sync

# 前端依赖
cd ui
npm install
```

### 步骤 2: 启动服务

**启动后端**
```bash
cd backend
python app/main.py
```

**启动前端（新终端）**
```bash
cd ui
npm run dev
```

### 步骤 3: 使用导出功能

1. 访问 http://localhost:3000
2. 进入任意项目
3. 点击「测试用例」菜单
4. 点击工具栏的「**导出**」按钮 📥
5. 选择格式（Excel/Word/JSON）
6. 点击「导出」确认
7. 等待文件自动下载

---

## 🎯 功能演示

### 批量导出（推荐）
1. 勾选要导出的测试用例
2. 点击「导出」按钮
3. 系统会导出选中的用例

### 全量导出
1. 不勾选任何用例
2. 点击「导出」按钮
3. 系统会导出所有用例

---

## 📊 导出格式说明

| 格式 | 适用场景 | 文件扩展名 |
|------|----------|------------|
| **Excel** | 导入其他测试工具（禅道、Jira） | .xlsx |
| **Word** | 文档归档、打印、评审 | .docx |
| **JSON** | 程序处理、数据交换、API集成 | .json |

---

## 🔍 导出内容

每条测试用例包含：
- ✅ 用例编号（TC-XXX）
- ✅ 用例标题
- ✅ 所属模块
- ✅ 用例类型（功能/性能/安全等）
- ✅ 优先级（低/中/高/关键）
- ✅ 前置条件（逐条列出）
- ✅ 测试步骤（带序号）
- ✅ 预期结果（与步骤对应）
- ✅ 备注信息

---

## 💡 使用技巧

### 技巧 1: 按文件夹导出
1. 点击左侧文件夹树选择文件夹
2. 右侧列表会过滤该文件夹下的用例
3. 点击「导出」即可导出该文件夹用例

### 技巧 2: 按优先级导出
1. 使用筛选器选择优先级（如：高优先级）
2. 点击「导出」导出筛选后的用例

### 技巧 3: 搜索后导出
1. 在搜索框输入关键词
2. 点击「导出」导出搜索结果

---

## 🐛 常见问题

### Q: 点击导出后没有反应？
**A**: 检查浏览器控制台是否有错误，确认后端服务正常运行

### Q: 导出的文件在哪里？
**A**: 文件会自动下载到浏览器默认下载目录（通常是「下载」文件夹）

### Q: 可以同时导出多个格式吗？
**A**: 目前一次只能导出一种格式，可以多次点击导出选择不同格式

### Q: 导出需要多长时间？
**A**: 
- 10条以内：即时完成（<1秒）
- 10-100条：几秒钟
- 100-1000条：10-30秒
- 1000条以上：可能需要1-2分钟

### Q: 导出失败怎么办？
**A**: 
1. 刷新页面重试
2. 检查后端日志查看错误信息
3. 确认数据库连接正常
4. 联系技术支持

---

## 📞 技术支持

遇到问题？

1. **查看日志**
   - 后端日志：终端输出
   - 前端日志：浏览器控制台（F12）

2. **检查文件**
   - 导出的文件保存在 `backend/exports/` 目录
   - 可以直接在该目录查看生成的文件

3. **测试导出器**
   ```bash
   cd backend
   python test_simple_export.py
   ```

---

## 🎉 完成！

导出功能已成功实施，享受便捷的测试用例导出体验吧！

**下一步建议**:
- 尝试导出不同格式的文件
- 测试大批量导出性能
- 向团队推广导出功能